function edge_jump(){
    $(window).scrollTop($("#div_edge").offset().top);
}

function threshold_jump(){
    $(window).scrollTop($("#div_threshold").offset().top);
}


function area_jump(){
    $(window).scrollTop($("#div_area").offset().top);
}


function statics_jump(){
    $(window).scrollTop($("#div_statics").offset().top);
}

function pic_jump(){
    $(window).scrollTop($("#div_pics").offset().top);
}

function lsf_jump(){
    $(window).scrollTop($("#div_lsf").offset().top);
}

function info_jump(){
    $(window).scrollTop($("#div_info").offset().top);
}

function fuzzy_jump(){
    $(window).scrollTop($("#div_fuzzy").offset().top);
}

function sj_jump(){
    $(window).scrollTop($("#div_sj").offset().top);
}
function fcn_jump(){
    $(window).scrollTop($("#div_fcn").offset().top);
}
function unet_jump(){
    $(window).scrollTop($("#div_unet").offset().top);
}
function deeplab_jump(){
    $(window).scrollTop($("#div_deeplab").offset().top);
}
function maskrcnn_jump(){
    $(window).scrollTop($("#div_maskrcnn").offset().top);
}

function intro_jump(){
    $(window).scrollTop($("#div_intro").offset().top);
}

function anno_jump(){
    $(window).scrollTop($("#div_anno").offset().top);
}

function result_jump(){
    $(window).scrollTop($("#div_result").offset().top);
}

function analysis_jump(){
    $(window).scrollTop($("#div_analysis").offset().top);
}

